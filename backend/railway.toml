# Use this ONLY if Railway Root Directory is set to "backend".
# Preferred: leave Root Directory empty (repo root) and use ../railway.toml instead.

[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "PYTHONPATH=.. uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT"
